{
  fun isEven(n: int): bool {
    if (n == 0) return true;
    return isOdd(n - 1); // expect error: Undefined variable.
  }

  fun isOdd(n: int): bool {
    if (n == 0) return false;
    return isEven(n - 1);
  }

  isEven(4);
}
