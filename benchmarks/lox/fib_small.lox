// Scaled-down variant of fib.lox for iterative Roxy benchmarking.
// fib(22) ≈ 35K fib calls; fib(35) in upstream is ~15M.
fun fib(n) {
  if (n < 2) return n;
  return fib(n - 2) + fib(n - 1);
}

var start = clock();
print fib(22) == 17711;
print clock() - start;
